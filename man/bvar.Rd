% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bvar.R
\name{bvar}
\alias{bvar}
\title{Fit a Bayesian multilevel VAR network model}
\usage{
bvar(
  id_col,
  time_col,
  y_cols,
  x_cols = NULL,
  center_x = FALSE,
  fe_interactions = NULL,
  re_interactions = NULL,
  re_cols = NULL,
  re_temporal = FALSE,
  K = 1,
  na_action = c("listwise"),
  skip_lag = TRUE,
  data,
  family = c("bernoulli", "ordinal", "gaussian"),
  priors = set_priors(),
  iter = 4000,
  warmup = 1000,
  chains = 4,
  cores = 1,
  seed = NULL,
  adapt_delta = NULL,
  max_treedepth = NULL,
  save_data = FALSE,
  save_fit = FALSE
)
}
\arguments{
\item{id_col}{Character. Name of the subject/group identifier column.}

\item{time_col}{Character. Name of the time column.}

\item{y_cols}{Character vector. Names of the outcome columns.}

\item{x_cols}{Character vector or NULL. Names of the covariate columns.}

\item{center_x}{Logical. Grand-mean centre covariates before fitting?
Default \code{FALSE}.}

\item{fe_interactions}{List or NULL. Fixed-effect interaction terms to add
to the design matrix. Each element is a character vector of column names
to interact, or \code{c("lag", "x")} to interact all lag columns with
a covariate.}

\item{re_interactions}{List or NULL. Random-effect interaction terms.}

\item{re_cols}{Character vector. Columns from X and/or "Intercept" to include as random slopes.}

\item{re_temporal}{Logical. Include random slopes on lag predictors?
Default \code{FALSE}.}

\item{K}{Integer. AR order. Default 1.}

\item{na_action}{Character. Missing-data strategy; currently only
\code{"listwise"}.}

\item{skip_lag}{Logical. If \code{TRUE} (default), rows with irregular time
gaps have their lag set to zero rather than being dropped.}

\item{data}{Data frame in long format.}

\item{family}{Character scalar or vector. Observation model per node.
A scalar is recycled to all \code{y_cols}. A vector of length
\code{length(y_cols)} (named or positional) specifies per-node families.
Valid values: \code{"bernoulli"}, \code{"ordinal"}, \code{"gaussian"}.}

\item{priors}{A \code{bvarnet_priors} object from \code{set_priors()}.
Defaults to \code{set_priors()} (package defaults).}

\item{iter}{Integer. Number of post-warmup iterations per chain. Default 4000.}

\item{warmup}{Integer. Number of warmup iterations per chain. Default 1000.}

\item{chains}{Integer. Number of MCMC chains. Default 4.}

\item{cores}{Integer. Number of chains to run in parallel. Default 1.}

\item{seed}{Integer or NULL. RNG seed.}

\item{adapt_delta}{Numeric in (0, 1). Target average proposal acceptance
probability during warmup adaptation. Higher values (e.g., 0.95–0.99)
reduce divergences at the cost of slower sampling. Default \code{NULL}
(CmdStan default of 0.8).}

\item{max_treedepth}{Integer. Maximum depth of the NUTS binary tree.
Increasing this allows the sampler to take more leapfrog steps per
iteration, which can help with difficult posteriors (e.g., funnels in
hierarchical logistic models) but increases computation. Default
\code{NULL} (CmdStan default of 10).}

\item{save_data}{Logical. If \code{TRUE}, store the preprocessed (sorted,
listwise-deleted) estimation data in the \code{data_used} slot of the
returned object for reproducibility and downstream analyses.
Default \code{FALSE}.}

\item{save_fit}{Logical. If \code{TRUE}, retain the live cmdstanr fit object
in the \code{stanfit} slot of the returned object instead of discarding it.
This enables calling cmdstanr methods such as \code{$log_prob()},
\code{$grad_log_prob()}, \code{$unconstrain_variables()}, and
\code{$unconstrain_draws()} (see Details). Default \code{FALSE}, which keeps
memory usage lean. For a homogeneous (single-family) model the slot holds one
\code{CmdStanMCMC} object; for a mixed-family model it holds a list of
per-node \code{CmdStanMCMC} objects. Requires the \pkg{cmdstanr} package.}
}
\value{
A \code{bvarnet} object (a named list) with slots:
\code{draws}, \code{convergence}, \code{diagnostics}, \code{timing},
\code{metadata}, \code{return_codes}, \code{family}, \code{standata},
\code{priors}. If \code{save_data = TRUE}, also includes
\code{data_used} (the cleaned estimation data frame). If
\code{save_fit = TRUE}, also includes \code{stanfit} (the retained cmdstanr
fit object, or a list of per-node fits for mixed families).
}
\description{
The \code{bvar} function estimates the posterior distribution of the specified Bayesian (Multilevel) Vector Autoregression.
}
\details{
\strong{Retaining the fit for \code{$log_prob()}.}
The package ships precompiled Stan executables (via \pkg{instantiate}) that do
not expose cmdstanr's standalone model methods. When \code{save_fit = TRUE},
\code{bvar()} recompiles the model from its bundled \code{.stan} source with
\code{compile_model_methods = TRUE} (a one-time compilation that needs a C++
toolchain) and stores the resulting fit in the \code{stanfit} slot. You can
then evaluate the model log density at arbitrary parameter values on the
\strong{unconstrained} scale, with no separate \code{$init_model_methods()}
call required:
\preformatted{
fit <- bvar(..., save_fit = TRUE)
sf  <- fit$stanfit
ud  <- sf$unconstrain_draws(format = "draws_matrix")
sf$log_prob(unconstrained_variables = ud[1, ], jacobian = TRUE)
sf$grad_log_prob(unconstrained_variables = ud[1, ], jacobian = TRUE)
}
For mixed-family models \code{fit$stanfit} is a list of per-node fits, so call
these methods on each node, e.g. \code{fit$stanfit[[j]]$log_prob(...)}. Note
that cmdstanr fits reference output CSV files on disk: \code{$log_prob()} works
within the same R session, but to persist the fit use
\code{sf$save_object(file)} rather than a bare \code{saveRDS()}.
}
\examples{
\dontrun{
# Run bvar on studentlife data
data(studentlife, package = "bvarnet")
fit <- bvar(
  id_col = "id",
  time_col = "time",
  y_cols = c("anxious", "calm", "conventional", "critical", "dependable"),
  re_temporal = TRUE,
  K = 1,
  data = studentlife,
  family = "ordinal",
  priors = set_priors(),
  seed = 1337)

summary(fit)
}
}
