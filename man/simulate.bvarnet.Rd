% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_predict.R
\name{simulate.bvarnet}
\alias{simulate.bvarnet}
\title{Simulate new trajectories from a fitted bvarnet model}
\usage{
\method{simulate}{bvarnet}(
  object,
  nsim = 20L,
  seed = NULL,
  method = c("posterior-mean", "posterior-sample"),
  ndraws = 10L,
  N = NULL,
  burnin = 200L,
  x_gen = NULL,
  subject_re = c("sample", "zero"),
  ...
)
}
\arguments{
\item{object}{A \code{bvarnet} object from \code{bvar()}.}

\item{nsim}{Integer. Number of time points per subject. Default 20.}

\item{seed}{Integer or NULL. RNG seed.}

\item{method}{Character. \code{"posterior-mean"} returns one data frame.
\code{"posterior-sample"} returns a list of data frames (one per draw).}

\item{ndraws}{Integer. Number of posterior draws when
\code{method = "posterior-sample"}. Default 10.}

\item{N}{Integer. Number of subjects. Defaults to J from the fitted model.}

\item{burnin}{Integer. Burn-in time points to discard. Default 200.}

\item{x_gen}{Function \code{f(N, T)} returning an \code{N x T x q} array
of covariates, or NULL for zero covariates.}

\item{subject_re}{Character. \code{"sample"} draws REs from
\code{N(0, sd_u)}. \code{"zero"} sets all REs to 0.}

\item{...}{Ignored.}
}
\value{
For \code{method = "posterior-mean"}: a data frame with columns
\code{id}, \code{t}, \code{y_1}, ..., \code{y_p} (and optional
\code{x_1}, ..., \code{x_q}). For \code{"posterior-sample"}: a list of
such data frames, one per draw.
}
\description{
Generates new multilevel VAR trajectories using the posterior parameters.
Reuses the existing response kernels from \code{sim_var()}.
}
\examples{
if (instantiate::stan_cmdstan_exists()) {
  sim <- sim_var(N = 5, T_obs = 30, p = 2, family = "gaussian", seed = 1)
  fit <- bvar(id_col = "id", time_col = "t",
              y_cols = c("y_1", "y_2"), x_cols = character(0),
              data = sim$data, family = "gaussian",
              iter = 200, warmup = 100, chains = 2, seed = 1)
  sim_data <- simulate(fit, nsim = 20, seed = 42)
}
}
