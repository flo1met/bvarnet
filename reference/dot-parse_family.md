# Parse and validate the family argument

Returns a named character vector of length `p` with names from `y_cols`.

## Usage

``` r
.parse_family(family, y_cols)
```

## Arguments

- family:

  Character scalar or vector.

- y_cols:

  Character vector of outcome names.

## Value

Named character vector.
