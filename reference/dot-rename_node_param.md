# Rename a per-node (p=1) Stan parameter name to full-model naming

Rename a per-node (p=1) Stan parameter name to full-model naming

## Usage

``` r
.rename_node_param(name, j, family, n_fe_full)
```
